# Progress Log

## Session 2026-04-26 (manual ralph mode — Claude Code session as orchestrator)

### Status
- Branch: main
- Order: #10 -> #2 -> #3 -> #4 -> #6 -> #5 -> #7 -> #8 -> #9
- Mode: subagent per issue, Opus orchestrator verifies

### 2026-04-26 — #10 done (provenance refactor)
- FactConfidence StrEnum (exact/grounded/inferred/human) replaces float on Provenance.
- model_self_score: float | None added (audit-only, never used for filtering).
- GraphNode.confidence + GraphEdge.confidence dropped entirely.
- LLMExtraction.confidence_floor dropped; require_grounding is the only LLM gate.
- SQLite migration 001_confidence_enum.py: idempotent table-rebuild; legacy llm_extraction rows mapped to 'grounded' (require_grounding gate already enforced upstream); raw float preserved into model_self_score.
- Edit API now writes FactConfidence.HUMAN.
- Verifiers: ruff clean, pyright 0 errors, pytest 22 passed / 8 Neo4j-gated skipped.
- Deviations: backend/retrieval/models.py rename deferred to retrieval issues; Neo4j Cypher migration to drop stale n.confidence/r.confidence on existing live DB not implemented (only new writes are clean).
- Next: #2 (R0 ContextEngine).

### 2026-04-26 — #2 done (R0 ContextEngine + cascade + eval harness)
- backend/retrieval/: ContextEngine, CascadeOrchestrator, Tier ABC, StubTier, Pydantic models (QueryResult.relevance, Hit.score — both algorithm-documented per #10).
- backend/eval/: golden.py (load_golden_set + key-based id extraction, ~97% coverage on tasks.jsonl), harness.py (run_eval, format_report_markdown, CLI __main__).
- API: POST /api/query wired with default orchestrator (StubTier only; tier impls follow in #3-#6).
- Verifiers: ruff clean, pyright 0 errors, pytest 70 passed / 8 Neo4j-skipped.
- backend/eval/reports/ added to .gitignore (generated artifact dir).
- Notes: tier registration is explicit list + matching TierConfig list (no name-magic); cascade fallback returns last tier's result not error; timeout_ms passed as hint via QueryContext.max_latency_ms.
- Next: #3 (R1 ExactTier).

### 2026-04-26 — #3 done (R1 ExactTier)
- backend/retrieval/exact.py: ExactTier(store, *, name, fulltext_limit). Reuses store._driver.
- backend/retrieval/index.py: ensure_indexes — CREATE FULLTEXT INDEX node_text IF NOT EXISTS on (n:Entity).attributes_json.
- Cascade wired: build_orchestrator_with_store(store) -> [ExactTier (escalate_below=0.5), StubTier (terminal)].
- API lifespan now uses build_orchestrator_with_store; storeless path retained for unit tests.
- Score docstring: id-match=1.0 ("Cypher exact-id"); fulltext score = Lucene BM25-similar squashed via score/(1+score).
- Tier triggers: id-token regex (emp_\d+, CLNT/CUST/VEND/ORG-\d+, ASIN, UUIDs, ticket|conv|order|sale|product[-_:]\w+); fulltext only if no id + <=4 tokens + no wildcards.
- Verifiers: ruff clean, pyright 0 errors, pytest 102 passed / 11 RUN_INTEGRATION-skipped.
- Open: p95 latency acceptance criterion needs RUN_INTEGRATION=1 with populated graph (eval-harness ready).
- Next: #4 (R2 HybridTier).

### 2026-04-26 — #4 done (R2 HybridTier)
- backend/retrieval/hybrid.py: HybridTier(store, embedder, *, candidate_limit=30, result_limit=10, rrf_k=60). Vector arm (Neo4j HNSW on :Entity.embedding) + fulltext arm (existing node_text) fused via RRF (k=60), normalized to [0,1].
- backend/retrieval/embedder.py: Embedder Protocol (dim, embed). StubEmbedder (hash-based, no semantic signal — for tests). BgeSmallEmbedder (lazy import sentence-transformers, BAAI/bge-small-en-v1.5).
- backend/retrieval/embed.py: one-shot CLI to populate :Entity.embedding (uv run python -m backend.retrieval.embed).
- backend/retrieval/index.py: ENTITY_VECTOR_INDEX="entity_vec", ensure_vector_index — idempotent CREATE VECTOR INDEX cosine.
- Cascade: [exact (escalate<0.5), hybrid (escalate<0.3), stub (terminal)]. QONTEXT_EMBEDDER env: stub|bge.
- Verifiers: ruff clean, pyright 0 errors, pytest 136 passed / 13 skipped.
- Deferred (logged in human-backlog.txt): sentence-transformers dep, reranker, embedding population, recall/latency benchmarks.
- Next: #6 (R4 RouterTier - GLiNER2).

### 2026-04-26 — #6 done (R4 RouterTier - Pioneer.ai GLiNER2)
- backend/retrieval/router.py: RouterTier(router, exact_tier, *, min_intent_conf=0.5, next_tier_for=...). EntityRouter Protocol + StubEntityRouter (regex) + GLiNER2EntityRouter (lazy gliner; raises if GLINER2_MODEL_PATH/PIONEER_AI_MODEL_ID unset).
- backend/retrieval/router_train/: prompt.md, seed_examples.jsonl (50), eval_set.jsonl (45), README.md (12-step Pioneer workflow + Pioneer side-challenge submission checklist).
- scripts/gen_router_seed.py: re-derive seeds from tasks.jsonl.
- Cascade: [exact, router, hybrid, stub]. Router placed after exact so id-token regex hits never spend 200ms NER budget.
- New: QueryResult.route_to: str | None — orchestrator honors at most once per query, never revisits a tier (3 new tests).
- Hit.score for RouterTier: forwarded verbatim from delegated ExactTier on lookup; zero hits + relevance=0.0 on search/analytical/ambiguous (router is a routing decision, not a retriever — documented).
- analytical → agentic (R3, not yet landed); orchestrator falls through to next-in-order if route_to unknown.
- gliner package NOT in pyproject (~700MB transitive); StubEntityRouter keeps CI green.
- Verifiers: ruff clean, pyright 0 errors, pytest 185 passed / 15 skipped.
- Pioneer.ai fine-tune logged in human-backlog.txt (12 steps, full materials prepared).
- Next: #5 (R3 AgenticTier - Gemini).

### 2026-04-26 — #5 done (R3 AgenticTier - Gemini function-calling)
- backend/retrieval/agentic.py: AgenticTier(store, embedder, llm, *, max_iterations=6, wall_clock_budget_ms=10_000). LLMClient ABC + LLMTurn/ToolCall/ToolResult dataclasses. StubLLMClient (scripted, tests), NoopLLMClient (no-key default), GeminiLLMClient (lazy google-genai, raises if GEMINI_API_KEY unset).
- backend/retrieval/tools.py: ToolBox with 6 tools — pattern_query (typed DSL via parse_pattern, no free Cypher), fulltext_search, vector_search, get_node, get_neighbors, get_source_record. CitationCollector dedups on (source_file, source_record_id, source_field). Per-tool size caps: _MAX_K=25, _MAX_DEPTH=3, _MAX_NEIGHBORS=50.
- Cascade: [exact, router, hybrid, agentic, stub] — agentic between hybrid and stub; router's route_to="agentic" lands here.
- Algorithmic relevance (per #10): RELEVANCE_GROUNDED=0.7 (final answer + ≥1 citation), RELEVANCE_UNGROUNDED=0.3 (answer no citations), RELEVANCE_FAILED=0.0 (timeout/overshoot/exception). NOT agent self-rating.
- Env: QONTEXT_AGENTIC=gemini|noop (default noop).
- Verifiers: ruff clean, pyright 0 errors, pytest 242 passed / 16 skipped.
- Human-backlog: GEMINI_API_KEY + QONTEXT_AGENTIC=gemini setup checklist.
- All 4 tiers + 1 stub now wired. Next: #7 (R5a Workflow framework).

---
