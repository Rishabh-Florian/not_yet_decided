# Progress Log

## Session 2026-04-26 (manual ralph mode — Claude Code session as orchestrator)

### Status
- Branch: main
- Order: #10 -> #2 -> #3 -> #4 -> #6 -> #5 -> #7 -> #8 -> #9
- Mode: subagent per issue, Opus orchestrator verifies

### 2026-04-26 — #10 done (provenance refactor)
- FactConfidence StrEnum (exact/grounded/inferred/human) replaces float on Provenance.
- model_self_score: float | None added (audit-only, never used for filtering).
- GraphNode.confidence + GraphEdge.confidence dropped entirely.
- LLMExtraction.confidence_floor dropped; require_grounding is the only LLM gate.
- SQLite migration 001_confidence_enum.py: idempotent table-rebuild; legacy llm_extraction rows mapped to 'grounded' (require_grounding gate already enforced upstream); raw float preserved into model_self_score.
- Edit API now writes FactConfidence.HUMAN.
- Verifiers: ruff clean, pyright 0 errors, pytest 22 passed / 8 Neo4j-gated skipped.
- Deviations: backend/retrieval/models.py rename deferred to retrieval issues; Neo4j Cypher migration to drop stale n.confidence/r.confidence on existing live DB not implemented (only new writes are clean).
- Next: #2 (R0 ContextEngine).

### 2026-04-26 — #2 done (R0 ContextEngine + cascade + eval harness)
- backend/retrieval/: ContextEngine, CascadeOrchestrator, Tier ABC, StubTier, Pydantic models (QueryResult.relevance, Hit.score — both algorithm-documented per #10).
- backend/eval/: golden.py (load_golden_set + key-based id extraction, ~97% coverage on tasks.jsonl), harness.py (run_eval, format_report_markdown, CLI __main__).
- API: POST /api/query wired with default orchestrator (StubTier only; tier impls follow in #3-#6).
- Verifiers: ruff clean, pyright 0 errors, pytest 70 passed / 8 Neo4j-skipped.
- backend/eval/reports/ added to .gitignore (generated artifact dir).
- Notes: tier registration is explicit list + matching TierConfig list (no name-magic); cascade fallback returns last tier's result not error; timeout_ms passed as hint via QueryContext.max_latency_ms.
- Next: #3 (R1 ExactTier).

### 2026-04-26 — #3 done (R1 ExactTier)
- backend/retrieval/exact.py: ExactTier(store, *, name, fulltext_limit). Reuses store._driver.
- backend/retrieval/index.py: ensure_indexes — CREATE FULLTEXT INDEX node_text IF NOT EXISTS on (n:Entity).attributes_json.
- Cascade wired: build_orchestrator_with_store(store) -> [ExactTier (escalate_below=0.5), StubTier (terminal)].
- API lifespan now uses build_orchestrator_with_store; storeless path retained for unit tests.
- Score docstring: id-match=1.0 ("Cypher exact-id"); fulltext score = Lucene BM25-similar squashed via score/(1+score).
- Tier triggers: id-token regex (emp_\d+, CLNT/CUST/VEND/ORG-\d+, ASIN, UUIDs, ticket|conv|order|sale|product[-_:]\w+); fulltext only if no id + <=4 tokens + no wildcards.
- Verifiers: ruff clean, pyright 0 errors, pytest 102 passed / 11 RUN_INTEGRATION-skipped.
- Open: p95 latency acceptance criterion needs RUN_INTEGRATION=1 with populated graph (eval-harness ready).
- Next: #4 (R2 HybridTier).

---
