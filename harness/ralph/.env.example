# Default Claude API — no config needed, just run ./ralph.sh <iterations>
# Add vars below only if your hackathon project needs them at loop time
# (e.g., DB_URL, API keys for the product being built).

# Example:
# DATABASE_URL=postgresql://localhost/hackathon
# OPENAI_API_KEY=...
